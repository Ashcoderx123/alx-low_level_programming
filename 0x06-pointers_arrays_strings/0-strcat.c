#include "main.h"
#include <string.h>

/**
 * _strcat - concatenate src to dest
 * @dest: string to use
 * @src: strings to use
 * Return: char
 */

char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (0);
}
